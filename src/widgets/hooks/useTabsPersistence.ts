// Tabs persistence functionality has been removed.
// Tabs will no longer be saved across page refreshes.

export const useTabsPersistence = () => {
  // Empty hook - tabs are not persisted to localStorage
};
